/** Shared evidence constants (leaf module — no local imports). */

export const EVIDENCE_SCHEMA_VERSION = 1 as const;

/** Approved evidence namespace path (M1 spec §4). */
export const EVIDENCE_RELATIVE_PATH = ".sureflow/evidence/evidence.jsonl" as const;

export const REDACTED = "[REDACTED]" as const;
